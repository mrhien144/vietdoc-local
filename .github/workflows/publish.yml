name: Publish VietDoc Local V2 Dev

on:
  push:
    branches:
      - v2-dev
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish V2 development image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ghcr.io/mrhien144/vietdoc-local:v2-dev
